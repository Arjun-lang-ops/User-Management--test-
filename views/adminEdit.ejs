<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Edit User</title>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <style>
        body {
            background: #f6f8fa;
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 420px;
            margin: 80px auto;
            background: #ffffff;
            padding: 32px;
            border-radius: 10px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.1);
        }

        h2 {
            text-align: center;
            margin-bottom: 25px;
            color: #374151;
        }

        label {
            display: block;
            margin-bottom: 6px;
            font-weight: 600;
            color: #374151;
        }

        input {
            width: 100%;
            padding: 10px 8px;
            margin-bottom: 18px;
            border: 1px solid #e5e7eb;
            border-radius: 5px;
            font-size: 1rem;
        }

        .actions {
            display: flex;
            justify-content: space-between;
            margin-top: 10px;
        }

        .btn {
            padding: 10px 18px;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-size: 0.95rem;
        }

        .btn-save {
            background: #38bdf8;
            color: #ffffff;
        }

        .btn-save:hover {
            background: #0ea5e9;
        }

        .btn-cancel {
            background: #e5e7eb;
            color: #374151;
            text-decoration: none;
            text-align: center;
            line-height: 36px;
        }

        .btn-cancel:hover {
            background: #d1d5db;
        }
    </style>
</head>
<body>

<div class="container">
    <h2>Edit User</h2>

    <form action="/admin/edit/<%= user._id %>" method="POST">
        <label>Username</label>
        <input 
            type="text" 
            name="username" 
            value="<%= user.username %>" 
            required
        />

        <label>Email</label>
        <input 
            type="email" 
            name="email" 
            value="<%= user.email %>" 
            required
        />

        <div class="actions">
            <a href="/admin/home" class="btn btn-cancel">Cancel</a>
            <button type="submit" class="btn btn-save">Update</button>
        </div>
    </form>
</div>

<% if (typeof error !== "undefined") { %>
<script>
    Swal.fire({
        icon: "error",
        title: "Update Failed",
        text: "Email already exists or invalid input"
    });
</script>
<% } %>

</body>
</html>
