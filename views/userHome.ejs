<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Home</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, Helvetica, sans-serif;
        }

        body {
            background-color: #f4f6f8;
            color: #333;
        }

        /* Navbar */
        .navbar {
            background-color: #4f46e5;
            padding: 15px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: white;
        }

        .navbar h1 {
            font-size: 22px;
        }

        .navbar a {
            color: white;
            text-decoration: none;
            margin-left: 20px;
            font-size: 15px;
        }

        .navbar a:hover {
            text-decoration: underline;
        }

        /* Hero section */
        .hero {
            height: 80vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 20px;
        }

        .hero-box {
            background: white;
            padding: 40px;
            border-radius: 10px;
            width: 100%;
            max-width: 600px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
        }

        .hero-box h2 {
            font-size: 28px;
            margin-bottom: 15px;
            color: #4f46e5;
        }

        .hero-box p {
            font-size: 16px;
            margin-bottom: 25px;
            line-height: 1.6;
        }

        .hero-box button {
            padding: 12px 25px;
            font-size: 15px;
            border: none;
            border-radius: 5px;
            background-color: #4f46e5;
            color: white;
            cursor: pointer;
        }

        .hero-box button:hover {
            background-color: #4338ca;
        }

        /* Footer */
        footer {
            text-align: center;
            padding: 15px;
            background: #e5e7eb;
            font-size: 14px;
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <div class="navbar">
        <h1>Home</h1>
        <div>
            <a href="/logout">Logout</a>
        </div>
    </div>

    <!-- Hero -->
    <div class="hero">
        <div class="hero-box">
            <h2>Welcome to the Home Page</h2>
            <p>
                This is your dashboard after successful login.
                You can manage your profile, view data, and explore features.
            </p>
            <button>Get Started</button>
        </div>
    </div>

    <!-- Footer -->
    <footer>
        © 2025 MyApp. All rights reserved.
    </footer>

</body>
</html>
